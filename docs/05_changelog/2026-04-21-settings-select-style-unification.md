# 2026-04-21 设置页下拉框样式统一

## 背景

设置面板中部分下拉框未挂统一输入类，仍显示为浏览器默认控件样式，与连接配置页的下拉框视觉不一致。

## 调整

- 在 `SettingsPanel.css` 中为设置面板内所有 `select` 补齐统一的深色输入框样式。
- 保持现有 JSX、状态、事件处理不变。
- 保留 `settings-select` 类，仅让它继承统一选择框基线。

## 验证

- `npx tsc --noEmit` 通过。
