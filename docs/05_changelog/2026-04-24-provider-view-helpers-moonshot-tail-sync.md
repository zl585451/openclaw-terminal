# Provider View Helpers Moonshot Tail Sync

## Summary

- Synced the remaining `moonshot` API-key mapping helpers from `feature/audiobook-workbench` into `main`.
- Added `MOONSHOT_API_KEY` support in `providerViewHelpers.ts` field mapping and visibility checks.

## Files

- `src/ui/settings/providerViewHelpers.ts`
