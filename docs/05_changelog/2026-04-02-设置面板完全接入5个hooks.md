# 2026-04-02 设置面板完全接入 5 个 hooks

## 变更

- **`useAdvancedSettings`**：`saveAdvancedSettings()` 新增 CSS 变量注入（`--text-sm` / `--text-base` 等），与面板 `apply()` 行为完全一致。
- **`useScreenshotShortcut`**：`saveShortcut()` 修正 fallback 逻辑（custom 为空时默认 `Alt+A`），与面板原逻辑对齐。
- **`SettingsPanel`**：接入 `useScreenshotShortcut` + `useAdvancedSettings`，移除手写的快捷键 state/useEffect、高级设置 state/useEffect；`apply()` 简化为调用 `saveShortcut()` + `saveAdvancedSettings()` + `saveApiKeys`。
- 5 个业务 hook 全部接入：`useApiKeys` · `useNocturneMemory` · `useAiLibrary` · `useScreenshotShortcut` · `useAdvancedSettings`。

## 验证

- `npx tsc --noEmit`、`npm test`
