# 2026-04-29 — Task 3：useCapabilityActions 单元测试

- 新增 `src/hooks/__tests__/useCapabilityActions.test.ts`（7 用例：`renderHook` + `vi.fn` 注入依赖）。
- 覆盖：跳过引导、能力栏 setup、send_prompt / image_studio 可用与不可用、`handleWelcomeAction(send_prompt)`、`insertImageToChat`。
