# 2026-04-02 设置面板重构计划步骤 5–6 收尾

## 步骤 5（导入与旧文件）

- 检索 `src/`：`ChatTab.v2.tsx` 只引用 `SettingsPanel`，不存在计划文中假设的「旧 Tab import 一行」修改点。
- 旧 `ConnectionTab.tsx` 等在步骤 3 已删除，无额外删除任务。

## 步骤 6（文档）

- 更新 `docs/04_dev_guides/设置面板重构计划.md`：标记步骤 5、6 完成，并修正「进行中」状态说明。

## 验证

- `npx tsc --noEmit`、`npm test`
