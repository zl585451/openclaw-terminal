# 2026-04-29 — Task 2：useOnboarding 单元测试

- 新增 `src/hooks/__tests__/useOnboarding.test.ts`（4 用例，`@testing-library/react` 的 `renderHook` / `act`）。
- 与源码一致：`localStorage` 键为 `oct.onboarding.dismissed`，值为 `'1'`。
