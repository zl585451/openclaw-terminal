# 2026-04-15 · 欢迎卡「画布」：仅打开侧栏 + 原样提示词

## 行为

- `src/ui/chat/ChatTab.v2.tsx`：`capabilityId === 'canvas'` 时仅 **`canvasBridge.openPanel()`**，发送内容 **仅为卡片上的 `prompt`**，**不再**向用户消息拼接任何引导前缀（P0 只验收触达与发送，模型后续行为另项）。

## 历史

- 曾短暂拼接中文引导句；按产品边界已移除，避免聊天气泡出现非卡面文案。
