# 文档：oct-gateway 架构概览

**日期：** 2026-04-29  
**分支：** `test/coverage-round2`

- 新增 `docs/02_architecture/GATEWAY_OVERVIEW.md`：网关一句话定位；启动命令与端口（WS 默认 18789、HTTP 默认 18790）；与历史中「streamChat/sessionManager/toolRouter」等文件名的对照；核心文件表；消息流转；Provider 清单（以 `providers.js`/`providerRouter.js`）；工具链路简述；dotenv、`config.json`、Google 分项、`~/.openclaw` legacy 及 `getEnvOrConfig` 优先级；高风险文件提示。
