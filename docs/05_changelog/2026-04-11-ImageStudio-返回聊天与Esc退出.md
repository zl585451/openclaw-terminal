# 2026-04-11 ImageStudio 返回聊天与 Esc 退出

## 本次改动

- Image Studio 顶部新增“返回聊天”按钮，用户可显式退出生图侧栏。
- 当 Image Studio 打开时，按 `Esc` 会直接关闭侧栏并返回聊天界面。

## 调整原因

- 生图侧栏是覆盖式右抽屉，仅靠输入区的小按钮切换，退出路径不够直观。
- 增加显式返回按钮和键盘快捷退出，降低首次使用的理解成本。
