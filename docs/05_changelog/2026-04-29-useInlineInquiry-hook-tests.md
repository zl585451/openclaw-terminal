# 变更：useInlineInquiry 单元测试

**日期：** 2026-04-29  
**分支：** `test/coverage-round2`

- 新增 `src/hooks/__tests__/useInlineInquiry.test.ts`，覆盖澄清卡多页表单（初始化、翻页、`openSpec`、`maybeTrigger`、草稿、跳过、提交、关闭、`reset`）。
- Vitest：`141` 用例全通过。
