# Claude Code 规则同步

- 将 `.cursor/rules/architecture-rules.mdc` 的前端/网关分层规则同步到 `CLAUDE.md`。
- 将 `.cursor/rules/multi-ai-workflow.mdc` 的多 AI 协作交接规则同步到 `CLAUDE.md`。
- 将 `.cursor/rules/local-doc-sync-preference.mdc` 的文档回填偏好同步到 `CLAUDE.md`。
