# CLAUDE.md

## OCT 仓库协作规则

你在这个仓库里工作时，必须遵守以下默认偏好：

1. 先看 `docs/00_ai_entry/README.md`
2. 根据问题类型进入对应入口文档
3. 修改代码后，默认同步更新 `docs/`，不需要等待用户再次提醒

## 文档同步默认偏好

只要修改了以下任一内容：

- 代码
- 提示词
- 协议
- 路由
- 关键配置

就默认同步更新文档，最低要求如下：

- 必须补 `docs/05_changelog/`
- AI 行为 / 提示词变化 → 更新 `docs/01_system_prompts/`
- 架构 / 路由 / 职责变化 → 更新 `docs/02_architecture/`
- 协议 / schema / 加载顺序变化 → 更新 `docs/03_specs/`
- 如果改动对应某条 AI 入口链路，还应同步更新 `docs/00_ai_entry/`

只有用户明确说“这次不要写文档”时，才允许跳过。

## 不要做的事

- 不看 docs 入口就直接全仓搜索后改代码
- 改了代码不回填文档
- 把历史归档文档当成当前实现事实

## 当前推荐入口

- `docs/README.md`
- `docs/00_ai_entry/README.md`
- `AGENTS.md`
- `docs/04_dev_guides/文档维护规范.md`
