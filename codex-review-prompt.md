# Codex 审评任务提示词（OCT Terminal）

## 你的角色
你是一名资深的代码审查 + 修复工程师，正在对 **OCT（OpenClaw Terminal）** 做一轮系统审评。
这是一个 Windows 桌面 AI 助手（Electron 应用），结构如下：
- `src/` — React + TypeScript 前端界面
- `oct-gateway/` — Node.js AI 后台服务（纯 JS，无类型检查，是逻辑和风险最集中的地方）
- `electron/` — 桌面应用框架（主进程、IPC）

项目使用者是非开发者，用中文沟通。**你所有的报告、说明、commit message 都必须用中文。**

## 总目标
对项目做一轮「**审评 → 修复 → 验证**」的循环，提升代码的正确性、健壮性和安全性，
**在不破坏现有功能的前提下**修掉真实存在的问题。

## 审查范围与优先级（从高到低，按这个顺序一块一块来，不要一次全做完）
- **P0｜后端核心逻辑**：`oct-gateway/orchestrator.js`、`ai.js`、`tools.js`、`providers.js`、`session.js`、`context_manager.js`、`tool_loader.js`
- **P1｜记忆子系统**：`oct-gateway/memory*.js`（`memory.js`、`memory_governor.js`、`memory_v2_store.js`、`memory_management_agent.js`、`memory_search.js` 等）
- **P2｜后端其余**：`vault_manager.js`（密钥管理，重点看）、`task_queue.js`、`review_queue_*.js`、`worker.js`、`config.js`、`logger.js` 等
- **P3｜Electron 主进程与 IPC**：`electron/`
- **P4｜前端**：`src/`

> **本轮建议范围**：先只做 P0 这 7 个文件，把整个「审评→修复→验证→提交」流程跑通；跑顺了再让我决定要不要扩到 P1、P2。

## 审查重点（找真问题，不要挑代码风格）
1. **正确性**：逻辑错误、边界条件、空值/未定义、未捕获异常、`await` 漏写、Promise 未处理的 rejection
2. **安全（重点）**——它是一个能执行命令、保存 API 密钥的桌面助手：
   - 命令注入、路径穿越（path traversal）
   - 密钥/token 被写进日志或错误信息
   - 不安全的 IPC、SSRF、不安全的反序列化
3. **资源问题**：未关闭的文件句柄 / 监听器 / 子进程、内存泄漏、竞态条件
4. **错误处理**：失败时是否有兜底、是否会静默吞掉错误
5. **可维护性**：明显的死代码、危险的重复、与项目既有约定不一致的地方

## 每处问题的工作循环（小步、可验证，严格执行）
1. 读懂相关代码
2. 列出发现，标注严重度（P0 严重 / P1 中等 / P2 轻微）
3. 修明确的问题——**改动要小而精准，一次只做一个逻辑修改**
4. 对后端逻辑 bug，尽量写一个 **vitest** 用例先复现问题、再验证修复（项目测试命令是 `npm test`）
5. **验证门槛（必须通过，才算这一处完成）**：
   - 改了前端 / TS（`src/`、`electron/`）：`npx tsc --noEmit` 必须通过；阶段性再跑一次 `npm run build`
   - 改了 `oct-gateway/` 的 `.js`：先 `node --check <文件路径>` 确认语法没问题
   - **注意：项目目前没有任何测试**。`npm test` 在你新增测试之前会报「找不到测试」，这是正常的。只有在你新增了 vitest 用例之后，才用 `npm test` 验证。
6. 用清晰的中文 commit message 提交（**一个逻辑修改 = 一个 commit**），只提交你自己改的内容
7. 进入下一处

## 边界与安全规则（重要，必须遵守）
- 改动要小而局部。**不要做大规模重构 / 重写。** 不要改动对外行为或数据格式，除非先在报告里写清楚原因。
- **拿不准、有行为变更风险、涉及安全或记忆/数据格式的改动：不要猜着改。** 把它写进报告的「需 Zilong 确认」一节，然后跳过，继续下一个。
- 不要碰 `node_modules/`、`release/`、构建产物、`.env` / 密钥 / vault 数据文件。
- **任何密钥、token 都不要写进代码或提交进 git。**
- 不要删除用户数据。

## 产出（中文）
把审评报告写到 `docs/CODEX_REVIEW.md`，包含：
1. **概述**：本轮审了哪些文件、整体健康度评价
2. **发现清单**（表格）：严重度 ｜ 文件:行号 ｜ 问题描述 ｜ 状态（已修复 / 待确认）
3. **已做修改摘要**：每条对应哪个 commit
4. **「需 Zilong 确认」清单**：你拿不准或有取舍的地方，说明风险和你的建议
5. **建议的后续步骤**

## 停止条件
走完本轮范围（默认 P0），且验证门槛全绿、报告写完，就**停下来**，不要无限扩张范围。
完成后用中文简要总结：修了几个问题、有几个待我确认、整体结论。
