---
description: 本地私有偏好 — 改代码后默认自动补 docs，不用再提醒
alwaysApply: true
---

# 本地文档回填偏好

这是本地协作偏好，不属于项目产品规则。

默认执行：

- 只要修改了代码、提示词、协议、路由或关键配置
- 就自动同步更新 `docs/`
- 不需要等待用户再次提醒

最低要求：

- 补 `docs/05_changelog/`
- AI 行为 / 提示词变化 → 更新 `docs/01_system_prompts/`
- 架构 / 路由 / 职责变化 → 更新 `docs/02_architecture/`
- 协议 / schema / 加载顺序变化 → 更新 `docs/03_specs/`

只有用户明确说“这次不要写文档”时，才允许跳过。
