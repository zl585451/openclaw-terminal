---
description: 动手前必做的三件事 — 读文档、理依赖、验范围
alwaysApply: true
---

# 动手前检查清单

## 第一步：先读相关文档

项目在 `docs/` 有完整的架构文档，修改前必须先查阅：

| 修改范围 | 必读文档 |
|---------|---------|
| 前端架构/流程 | `docs/02_architecture/` |
| 前后端协议 | `docs/03_specs/WEBSOCKET_PROTOCOL.md` |
| 渲染/Markdown | `docs/03_specs/RENDER_PROTOCOL.md` |
| 已知问题 | `docs/03_specs/99_known_issues.md` |
| 迁移状态 | `docs/03_migration/migration-status.md` |

**如果文档里说这个功能已经做过或有决策记录，不要重复实现。**

---

## 第二步：列出会影响哪些文件

修改前，先列出：

```
将要改动的文件：
- src/xxx/xxx.ts  （原因：...）

可能被影响的文件：
- src/yyy/yyy.ts  （因为它 import 了上面的文件）

不会改动的文件：（如果你以为不用改，说明为什么）
```

如果影响超过 5 个文件，必须先整体说明方案，等确认后再执行。

---

## 第三步：修改后验证

每次修改完，按顺序执行：

```bash
# 1. TypeScript 类型检查（前端）
npx tsc --noEmit

# 2. 运行单元测试
npm test

# 3. 如果改了网关，检查语法
node --check oct-gateway/index.js
```

有报错必须修复后才能声明"完成"。

---

## 第四步：同步更新文档（强制）

**任何代码修改必须同步更新 `docs/` 文件夹。**

### 文档更新检查清单

修改完成后，必须检查并更新以下文档：

| 文档路径 | 何时更新 | 更新内容 |
|---------|---------|---------|
| `docs/05_changelog/` | 每次提交 | 新增变更记录，格式：`YYYY-MM-DD-简述.md` |
| `docs/03_specs/` | 修改协议/接口时 | 更新协议规范、渲染规则等 |
| `docs/02_architecture/` | 修改架构时 | 更新架构图、数据流、模块关系 |
| `docs/04_dev_guides/` | 修改开发流程时 | 更新开发指南、使用说明 |
| `docs/01_system_prompts/` | 修改 AI 行为时 | 更新系统提示词、身份设定 |

### 提交前必须确认

```
□ 代码改动是否已在 changelog 记录？
□ 架构变更是否已更新架构文档？
□ 接口变更是否已更新协议文档？
□ 新增功能是否有对应的使用指南？
□ 删除的功能是否已在文档中标记废弃？
```

**未更新文档的提交将被要求补充后才能合并。**

---

## 禁止的操作

- ❌ 不读文档就直接改代码
- ❌ 修改超过 5 个文件但不先说明整体方案
- ❌ 把测试报错当作"不影响功能"跳过
- ❌ 在 `ChatTab.v2.tsx` 里新增功能（应该先拆分）
- ❌ **只改代码不改文档**
