---
description: 多 AI 协作规则 — 收到外部方案时先校验，不盲执行
alwaysApply: true
---

# 多 AI 协作规范

## 收到外部方案时（Claude/GPT 给的代码或建议）

先做这三件事：

1. 核对落点是否符合 `architecture-rules.mdc`
2. 核对影响范围和依赖关系
3. 先用中文复述“我要改什么、改哪些文件、预期效果是什么”，确认后再写代码

## 禁止直接执行的情况

以下情况必须先提问：

- 直接要求“把 Claude / GPT 给的代码原样加进去”
- 要修改的文件已经超过 300 行，且方案会继续膨胀
- 方案要求新建文件，但没有说明目录
- 同时改前端 `src/` 和网关 `oct-gateway/`

## Cursor 给出方案时的格式要求

每次给方案，至少包含：

```
【目标】一句话
【修改文件】
- src/xxx/xxx.ts — 原因
【不会改动】可选，但有价值时要写
【架构检查】符合分层 / 不符合（说明）
```
